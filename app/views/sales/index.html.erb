<p id="notice"><%= notice %></p>

<h1>Registro das vendas</h1>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Número</th>
      <th>Cliente</th>
      <th>Produto</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @sales.each do |sale| %>
      <tr>
    		<td><%= sale.id %></td>
    		<td><%= sale.client.fullname %></td> 
        <td><%= link_to 'Mostrar', sale %></td>
        <td><%= link_to 'Editar', edit_sale_path(sale) %></td>
        <td><%= link_to 'Apagar', sale, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nova Venda', new_sale_path, class: "btn btn-primary btn-lg", role: "button" %>
<%= link_to "Home", root_path, class: "btn btn-primary btn-lg", role: "button" %>
